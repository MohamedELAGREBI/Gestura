<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Gestura.Views.HomePage"
             Title="HomePage">

    <StackLayout Padding="20" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" Spacing="10">
        <Button Text="Sessions de Dessin"
                Command="{Binding NavigateToDrawingSessionCommand}"
                Style="{StaticResource PrimaryButtonStyle}"/>
        <Button Text="Galerie d'images"
                Command="{Binding NavigateToImageGalleryCommand}"
                Style="{StaticResource PrimaryButtonStyle}"/>
    </StackLayout>
    
</ContentPage>